package review;
class Cleanser {
    private String activeIngredient; // thành phần hoạt chất, chỉ dùng bên trong class

    public void dilute(int percent) {
        // Pha loãng chất tẩy theo tỉ lệ phần trăm
    }

    public void apply(DirtyThing d) {
        // Áp dụng chất tẩy lên vật bẩn
    }

    public void scrub(Brush b) {
        // Chà rửa với bàn chải
    }
}
